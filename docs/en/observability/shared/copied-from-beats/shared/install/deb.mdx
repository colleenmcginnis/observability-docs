

<DocIf condition={ "((release-state))" === "unreleased" }>

Version ((version)) of {props.beatname_uc} has not yet been released.

</DocIf>

<DocIf condition={ "((release-state))" !== "unreleased" }>

```sh
curl -L -O https://artifacts.elastic.co/downloads/beats/{props.beatname_lc}/{props.beatname_lc}-((version))-amd64.deb
sudo dpkg -i {props.beatname_lc}-((version))-amd64.deb
```

</DocIf>