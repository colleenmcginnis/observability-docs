import StopCommand from './stop-command.mdx'

The DEB package includes a service unit for Linux systems with systemd. On these
systems, you can manage ((agent)) by using the usual systemd commands.

<StopCommand />
