
import FleetManagedSettings from '../../configure/shared/fleet-managed-settings.mdx'

Create or update a secret token in ((fleet)).

<FleetManagedSettings />

1. Navigate to **Agent authorization** > **Secret token** and set the value of your token.
1. Click **Save integration**. The APM Server will restart before the change takes effect.
