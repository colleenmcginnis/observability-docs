---
slug: /en/observability
title: Elastic Observability
description: Description to be written
layout: landing
tags: []
---

import introData from './data/intro-data.js'
import recommendedData from './data/recommended-data.js'
import useCasesData from './data/use-cases-data.js'
import relatedDocsData from './data/related-docs-data.js'

<DocLandingHero
  title={introData.title}
  description={`${introData.description}`}
  display="align"
  image={
    {
      url: introData.image,
      alt: 'An illustration of an Observability dashboard.'
    }
  }
/>

<ul>
{introData.features && introData.features.map(feat => <li>{feat}</li>)}
</ul>

<br />

## {recommendedData.title}

{/* <DocTabs hasBorder>
  {recommendedData.tabs.map(topic => {
    return (
      <DocTab name={topic.title}>
        <DocRelatedArticles
          items={topic.items}
        />
      </DocTab>
    )
  })}
</DocTabs> */}

## Use cases

{/* {useCasesData.map((useCase, i) => {
  return (
    <div style={{
      width: "50%",
      float: "left",
      paddingBottom: "30px",
      paddingRight: (i === 0 || i === 2) ? "15px" : "0px",
      paddingLeft: (i === 1 || i === 3) ? "15px" : "0px"
    }}>
      <h3>{useCase.title}</h3>
      <p>{useCase.description}</p>
      {useCase.docs.map(doc => {
        return (
          <p>
            <div>
              <DocIcon type="document" />&nbsp;&nbsp;<DocLink id={doc.id}>{doc.linkText}</DocLink>
            </div>
            <div style={{ paddingLeft: "25px" }}>{doc.description}</div>
          </p>
        )
      })}
    </div>
  )
})} */}

<div style={{ clear: "both" }} />

## Related documentation

{/* <DocRelatedArticles
  items={relatedDocsData}
/> */}
